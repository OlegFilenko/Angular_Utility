namespace Angular_Utility.Enums {

    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    //=====| NgElement |======================================================================================================>>
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    #region enum NgElement

    public enum NgElement {
        component,
        module,
        html,
        style,
        service,
        model,
        directive,
        moduleRouting,
        validator,
        guard,
        interceptor,
        animation,
        ngEnum,
        ngClass,
        ngInterface
    }

    #endregion
}
